let initialState = {
  friends: [
    { name: "Sasha", id: 1 },
    { name: "Vika", id: 3 },
    { name: "Oleg", id: 5 },
  ]
};

export const navbarReducer = (state = initialState, action) => {
  return state;
};

export default navbarReducer;
